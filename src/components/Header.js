import React, { Fragment } from 'react';

const Header = () => (
    <Fragment>
        <header className="header">
            I am from Header!!!
        </header>
    </Fragment>
);

export default Header;